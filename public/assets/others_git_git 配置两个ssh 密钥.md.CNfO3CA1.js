import{_ as a,c as n,a0 as p,o as e}from"./chunks/framework.CoVXEd1Z.js";const u=JSON.parse('{"title":"git 配置两个ssh 密钥","description":"","frontmatter":{},"headers":[],"relativePath":"others/git/git 配置两个ssh 密钥.md","filePath":"others/git/git 配置两个ssh 密钥.md","lastUpdated":1734360942000}'),t={name:"others/git/git 配置两个ssh 密钥.md"};function i(l,s,o,c,h,g){return e(),n("div",null,s[0]||(s[0]=[p(`<h1 id="git-配置两个ssh-密钥" tabindex="-1">git 配置两个ssh 密钥 <a class="header-anchor" href="#git-配置两个ssh-密钥" aria-label="Permalink to &quot;git 配置两个ssh 密钥&quot;">​</a></h1><p>我自己有一个<code>github</code>账号，在进入公司之后，公司的代码也是存储在<code>github</code>上面。因为我是用自己电脑开发，所以我需要向两个代码仓库中拉取和提交代码。</p><p>这就导致如果你拿你原来的公钥给公司添加仓库访问权限的时候是拉取不到代码的，因为一个<code>ssh</code> 公钥只能用于一个账户。</p><p>这就需要配置两个 <code>ssh</code> 密钥</p><h2 id="具体步骤" tabindex="-1">具体步骤 <a class="header-anchor" href="#具体步骤" aria-label="Permalink to &quot;具体步骤&quot;">​</a></h2><ol><li><p>在~/.ssh 目录下新建一个github 目录</p></li><li><p>执行以下命令</p><p>生成个人<code>ssh</code> 密钥</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ssh-keygen -t rsa -C &quot;personal@github.com&quot; -f ~/.ssh/github/personal.github</span></span></code></pre></div><p>生成公司<code>ssh</code> 密钥</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ssh-keygen -t rsa -C &quot;work@github.com&quot; -f ~/.ssh/github/work.github</span></span></code></pre></div><p>添加配置文件，在.ssh目录下新增<code>config</code>文件，内容如下:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span># 个人</span></span>
<span class="line"><span>Host personal.github.com</span></span>
<span class="line"><span>    Hostname ssh.github.com</span></span>
<span class="line"><span>    port 22</span></span>
<span class="line"><span>    User git</span></span>
<span class="line"><span>    IdentityFile ~/.ssh/github/personal.github</span></span>
<span class="line"><span></span></span>
<span class="line"><span># 公司</span></span>
<span class="line"><span>Host work.github.com</span></span>
<span class="line"><span>    Hostname ssh.github.com</span></span>
<span class="line"><span>    port 22</span></span>
<span class="line"><span>    User git</span></span>
<span class="line"><span>    IdentityFile ~/.ssh/github/work.github</span></span></code></pre></div></li><li><p>将生成的公钥文件添加到个人github账户或者公司的github账户。</p><p>例如：复制<code>personal.github.pub</code>文件里面的内容，</p><p>登陆自己的github账户-&gt;setting-&gt;SSH and GPG keys-&gt;new ssh key。</p></li><li><p>拉取仓库代码：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>//拉取自己git仓库代码</span></span>
<span class="line"><span>//原来是这样拉取</span></span>
<span class="line"><span>git clone git@github.com:lltengmu/docs.git</span></span>
<span class="line"><span>//现在是这样拉取</span></span>
<span class="line"><span>git clone personal.github.com:lltengmu/docs.git</span></span></code></pre></div><p>如果是拉取公司项目代码就：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>git clone work.github.com:xxx/xxx.git</span></span></code></pre></div></li><li><p>提交没有变化，正常提交就行</p></li></ol>`,6)]))}const r=a(t,[["render",i]]);export{u as __pageData,r as default};
